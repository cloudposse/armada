# create build directory
#GIT_DIR=$(mktemp -d -t "$username_$repository_$revision")
GIT_DIR="/tmp/$username_$repository_$revision.$$"

mkdir -p "$GIT_DIR"

echo "Extracting git archive to $GIT_DIR"

# change to work dir
cd $GIT_DIR

# tar extract
tar x


